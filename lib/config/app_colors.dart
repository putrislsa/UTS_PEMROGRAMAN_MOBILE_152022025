import 'package:flutter/material.dart';

/// WARNA & TEMA — Light / Neutral
class AppColors {
  // Dasar netral
  static const Color bg = Color(0xFFF7F8FA); // very light gray
  static const Color card = Colors.white; // kartu putih
  static const Color primary = Color(0xFF3B4857); // slate 700 (aksen netral)
  static const Color primarySoft = Color(0xFF607089); // slate 500–600
  static const Color border = Color(0xFFE5E7EB); // gray 200
  static const Color focus = Color(0xFF94A3B8); // slate 400 (focus ring)

  // Teks
  static const Color textPrimary = Color(0xFF111827); // gray 900
  static const Color textSecondary = Color(0xFF6B7280); // gray 500
}
